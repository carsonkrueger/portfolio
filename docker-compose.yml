services:
    portfolio:
        container_name: portfolio
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "5000:5000"
        env_file:
            - "back-end/.env"
